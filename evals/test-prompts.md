# evals/test-prompts.md — Тестовые запросы для проверки роутинга

> Цель: под каждый скилл — минимум один реальный запрос, который должен триггерить
> именно его (и явно НЕ триггерить соседние домены, если это важно проверить).
> Формат и обязательность — `CONTRIBUTING.md` шаг 7 («Добавить новый скилл»),
> `SCHEMA.md` раздел 8 (чеклист готовности скилла).
>
> **Как проверять:** прогнать запрос через `ROUTER.md` (таблица триггеров + алгоритм
> по шагам) и свериться, что домен(ы) в колонке «Ожидается» совпадают с тем, что
> реально загрузит роутер. При изменении `description` в frontmatter скилла —
> перепрогнать связанные строки.

---

## Формат записи

| Запрос | Ожидается | Комментарий |
|---|---|---|
| _текст запроса на русском, как реально спросил бы дизайнер_ | `domain-1`, `domain-2` | что именно проверяет этот кейс |

---

## `product-design`

| Запрос | Ожидается | Комментарий |
|---|---|---|
| «ПМ дал задачу поправить график работы сотрудников, но неясно: можно ли выбрать сразу нескольких сотрудников и изменить у всех график на день, можно ли выбрать несколько дней сразу, можно ли трогать прошедшую дату» | `product-design` | Проверяет триггер «требования» / размытое ТЗ — скилл должен вести к уточняющим вопросам (Метод, шаг 1), а не к готовому решению. |
| «Какой должен быть user flow для оформления отпуска сотрудником, если у HR-админа и сотрудника разные шаги согласования?» | `product-design` (+ `frameworks/jtbd.md` при нескольких ролях) | Проверяет триггер «user flow» + нелинейный сценарий (принцип №5, обязателен flow chart) + разбор по ролям. |
| «Спроектировать фичу уведомлений на основе результатов ресёрча по онбордингу» | `ux-research` → `product-design` → `design-systems` | Проверяет цепочку из `ROUTER.md`: скилл не подменяет ресёрч, подключается после него, и передаёт эстафету в design-systems на компонентном уровне. |
| «Собери спеку компонента Date picker — какой вариант лучше» | `design-systems` (не `product-design`) | Негативный кейс: запрос уже на уровне компонента, а не флоу/фичи — должен уйти в design-systems, не сюда. |

**Статус проверки:** сверено вручную с таблицей триггеров `ROUTER.md` и `description` в `skills/product-design/SKILL.md` — формулировки запросов пересекаются с триггерными словами домена (флоу, user flow, требования, фича, сценарий), негативный кейс (компонент) явно выведен в `design-systems`. Прогон через реальный роутер в отдельной сессии Zhandos — не выполнялся.

---

## `design-systems`

| Запрос | Ожидается | Комментарий |
|---|---|---|
| «Есть ли в нашей DS компонент под мультиселект сотрудников, или делать локальный?» | `design-systems` | Проверяет ветку «использовать / расширить / создать локальный / эскалировать» (Часть A метода). |
| «Проверь этот инпут на соответствие DS — кажется, бордер и цвет текста не из токенов» | `design-systems` | Проверяет DS-audit (Часть B метода) — разбор «элемент → проблема → параметр». |
| «Разбить Date/Month/Year picker на отдельные компоненты или держать единым Calendar?» | `design-systems` | Реальный кейс из «Примеры» самого скилла — дробление vs объединение компонента. |
| «Какой должен быть флоу оформления отпуска сотрудником?» | `product-design` (не `design-systems`) | Негативный кейс: вопрос про флоу/фичу целиком, не про конкретный компонент — граница из `related: product-design`. |

## `ux-writing`

| Запрос | Ожидается | Комментарий |
|---|---|---|
| «Что должна сказать кнопка отправки заявки на отпуск?» | `ux-writing` | Проверяет функцию «объясняет, что произойдёт» (CTA) — Шаг 1–2 метода. |
| «Текст ошибки "Что-то пошло не так" — перепиши, он бесполезный» | `ux-writing` | Проверяет анти-паттерн из стоп-листа `brand-tone.md`, функцию «сообщает результат». |
| «Что написать в empty state, если фильтр по сотрудникам ничего не нашёл?» | `ux-writing` | Проверяет функцию «заполняет пустоту». |
| «Напиши текст для рекламного лендинга нового продукта» | вне scope — не `ux-writing` | Негативный кейс: маркетинговый копирайтинг явно исключён («не покрывает: маркетинговый копирайтинг, продающие лендинги»); скилл должен честно отказать, не пытаться натянуть UI-метод. |

## `ux-research`

| Запрос | Ожидается | Комментарий |
|---|---|---|
| «Не уверен, что мы правильно понимаем, зачем HR нужен месячный вид графика — как это проверить?» | `ux-research` | Проверяет формулировку research question (Шаг 1) вместо угадывания. |
| «Нужно протестировать прототип нового флоу отпуска — каким методом?» | `ux-research` | Проверяет выбор метода (Шаг 2) — usability test под конкретный вопрос. |
| «У меня транскрипты пяти интервью с HR — как вытащить из них инсайты?» | `ux-research` → `mirrors/anthropic/research-synthesis` | Проверяет переход к synthesis (Шаг 5) — скилл ведёт до синтеза, дальше зеркало. |
| «Какую метрику поставить на фичу автозаполнения графика?» | `product-analytics` (не `ux-research`) | Негативный кейс: вопрос про метрики, а не про понимание пользователя — явная граница «Скилл не покрывает продуктовую аналитику». |

## `product-analytics`

| Запрос | Ожидается | Комментарий |
|---|---|---|
| «Какую метрику поставить на фичу автозаполнения графика?» | `product-analytics` | Базовый триггер — выбор метрики под фичу. |
| «Это хорошая метрика — количество кликов по кнопке "Создать график"?» | `product-analytics` | Проверяет чек-лист «плохой метрики» — vanity-метрика / метрика-активность. |
| «Помоги собрать фаннел: выбрал тип графика → настроил → прикрепил сотрудников → сохранил» | `product-analytics` | Проверяет фреймворк «Фаннел» для многошагового сценария. |
| «Посчитай нужный размер выборки для A/B-теста этой фичи» | вне scope — не `product-analytics` | Негативный кейс: расчёт выборки — статистика, explicitly «скилл не покрывает»; должен честно сказать, что нужен аналитик. |

## `ux-ui-theory`

| Запрос | Ожидается | Комментарий |
|---|---|---|
| «Почему этот экран ощущается тяжёлым, хотя вроде всё на месте?» | `ux-ui-theory` | Базовый триггер диагностики «почему тяжело». |
| «В выпадающем списке 30 несгруппированных пунктов — пользователи долго выбирают, почему?» | `ux-ui-theory` | Проверяет семью «Законы» (закон Хика) — Шаг 2–3 метода. |
| «Пользователи промахиваются мимо маленькой кнопки в углу — это что, закон какой-то?» | `ux-ui-theory` | Проверяет семью «Законы» (закон Фиттса). |
| «Экран выглядит перегруженным — слишком много цвета и разных шрифтов» | `graphic-design` (не `ux-ui-theory`) | Негативный кейс: «выглядит плохо» — это визуальная реализация (graphic-design), не когнитивный принцип; граница явно прописана в описании обоих скиллов. |

## `graphic-design`

| Запрос | Ожидается | Комментарий |
|---|---|---|
| «Экран ощущается не так, но не могу понять, что именно» | `graphic-design` | Базовый триггер — визуальная проблема без ясной причины. |
| «Поправь визуальную иерархию на экране списка сотрудников» | `graphic-design` | Проверяет Шаг 2 метода — три уровня иерархии. |
| «Слишком много цвета на экране, рябит в глазах» | `graphic-design` | Проверяет Шаг 5 метода — цвет и контраст. |
| «Сделай постер для внутреннего HR-мероприятия» | вне scope — не `graphic-design` | Негативный кейс: постеры прямо исключены («не покрывает: постеры, баннеры... иллюстрации»), пересекается с anti-cred Zhandos. |

## `branding`

| Запрос | Ожидается | Комментарий |
|---|---|---|
| «Это решение в бренде или нет — логотип нормально смотрится на этом экране?» | `branding` | Базовый триггер — проверка узнаваемости. |
| «Бренд говорит одно, дизайн-система — другое. Что главнее?» | `branding` | Проверяет Шаг 2 метода — разведение слоёв бренд vs DS. |
| «Показываем этот экран клиенту Astana Motors — выглядит ли это как MyStartups?» | `branding` | Проверяет Шаг 5 метода — двойственный контекст AM / рынок. |
| «Придумай название для новой фичи автозаполнения графика» | вне scope — не `branding` | Негативный кейс: нейминг явно вне scope («Вне scope — нейминг» в разделе «Связи»). |

---

## Итог по покрытию

Все 8 доменных скиллов (`product-design`, `design-systems`, `ux-writing`, `ux-research`,
`product-analytics`, `ux-ui-theory`, `graphic-design`, `branding`) имеют минимум 3
позитивных кейса и 1 негативный на границу с соседним доменом — чеклист
`CONTRIBUTING.md` шаг 7 закрыт для всех текущих скиллов.

**Не проверено:** реальный прогон через рабочую сессию с роутером. Все записи —
ручная сверка формулировки запроса с `description` во frontmatter и разделом
«Когда использовать» / «Скилл не покрывает» каждого `SKILL.md`. Домен `meta`
(`mirrors/anthropic/design-critique`, `accessibility-review`, `design-handoff`) —
тестовыми запросами не покрыт, отдельная задача.
