---
name: skill-name                         # kebab-case, совпадает с именем директории
description: Use this skill whenever ... # только EN, «pushy»: что делает + когда использовать + типовые формулировки запросов + чего НЕ покрывает. См. пример в SCHEMA.md §2
version: 1.0.0                           # semver
language: ru                             # ru | en | bilingual
domain: domain-name                      # один из 9 доменов, список — SCHEMA.md §2
status: draft                            # новый скилл всегда начинает с draft
last_updated: YYYY-MM-DD
depends_on: []                           # skill-name, без которых скилл не работает
related: []                              # локальные — по имени; зеркала — mirrors/anthropic/<name>
tags: []
mirrors: null                            # всегда null; зеркала определяются по ATTRIBUTION.md
---

<!--
Template: Skill Skeleton
Когда использовать: создание нового SKILL.md (CONTRIBUTING.md §1, шаг 3).
Формат и правила: SCHEMA.md — frontmatter (§2), секции тела (§3), длина до 300 строк (§4), стиль (§5).
Как использовать: скопируй файл в skills/<domain>/SKILL.md, заполни frontmatter и секции,
удали этот комментарий и подсказки-цитаты. Перед коммитом — чеклист SCHEMA.md §8.
-->

# <Название скилла> — на русском

## Когда использовать

> Конкретные триггеры с примерами формулировок — как реально спросил бы дизайнер.

- «…»
- «…»
- На ревью замечание звучит как «…».

**Скилл не покрывает:** … (→ `<домен-сосед>`), … (→ `mirrors/anthropic/<name>`). Явно перечисли границы с соседними доменами — по ним работает арбитраж роутинга (PROJECT_INSTRUCTIONS.md, Шаг 2).

## Что делает

> 2–4 предложения: что скилл делает и что на выходе.

## Контекст и принципы

> Базовые принципы, на которых строится метод. Командные ограничения — со ссылками на `team/*`, без пересказа фактов, которые уже грузятся из team-слоя.

## Метод

> Пошаговый процесс. Каждый шаг: что делать, что искать, что выдавать.

1. …
2. …
3. …

## Формат вывода

> Что должен выдать LLM. Ссылка на `templates/<name>.md`, если деливерабл шаблонизирован.

## Анти-паттерны

> Чего НЕ делать. Конкретные ошибки, не абстракции.

- …

## Примеры

> Хороший → плохой. Объёмные — выноси в `examples/`.

## Связи

- **Predecessor:** `<skill-name>` — когда вызвать перед.
- **Successor:** `<skill-name>` — когда вызвать после.
- **Conflicts:** `<skill-name>` — когда не комбинируется.

## Источники

- …
