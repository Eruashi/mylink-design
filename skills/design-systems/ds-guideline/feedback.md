# Фидбек: ds-guideline

Заполняется после каждого реального прогона. Раз в 3–5 записей Claude Code предлагает дифф в `SKILL.md` со ссылками на записи.

## 2026-09-26 — Modal header `17100:18553` (тестовый прогон)
- **Что сработало:** 13/13 секций, 9 на фактах Figma; найдены 5 вопросов к компоненту (мёртвые свойства по Align/Type, крестик поверх длинного Title, `OLD Badge` в статусе, расхождения паддингов, правило строк Title); найден свободный гайд `19442:22424`, которого нет в реестре.
- **Что поправил руками:** визуал гайдлайна в Figma не понравился. Лучший пример визуала на сейчас (не эталон — будем дорабатывать вместе) — гайд «Tabs/Segment controls» `19500:12562` (ветка `bRwFkumYDoKh1OQv5a5dYp`): синяя обложка с превью, крупные заголовки, примеры в светлых контейнерах с подписями, компактные таблицы, жёлтая плашка-предупреждение, ширина ~936. Структура из 13 секций пока остаётся — обсудим отдельно.
- **Чего не хватило:**
  - где писать строку покрытия; как считать «Не применимо» и «на фактах»;
  - нет места для итогов аудита токенов; 06 без собственных состояний компонента; что считать «элементом» в 12;
  - поиск инстансов — `getInstancesAsync()` у вариантов (вписать в метод); лёгкий режим аудита (только summary);
  - свободный гайд на странице при «нет» в реестре; обновление `figma-status/` в конце; как помечать мёртвые свойства;
  - формат пометки «нет токена» в примере SKILL.md не совпадает с шаблоном.
- **Предложение для скилла:** закрыть в 0.2.0 вместе с правками после ревью гайдлайна.
- **Применено в версии:** 0.2.0 (новая структура из 10 разделов, пункты выше)

## 2026-09-26 — Modal header, пересборка по 0.2.0
- **Что сработало:** 10 разделов, все 26 TODO сохранены; 13 рядов примеров в «Свойствах»; спецификация по размерам; таблица Figma → Flutter.
- **Что поправил руками:** — (ждёт ревью Zhandos)
- **Чего не хватило:**
  - гайд стал длиннее (15161 против 9657) — нет правила компактности рядов (сетка 2–3 примера в ряд, объединять простые свойства);
  - куда класть спецификацию Mobile по размерам (8 — только web, 9 — Flutter);
  - шаблон противоречит правилу «таблицы только в 8–9» (таблицы в 2 и 7); нет места для «Как добавить», аудита токенов, строки покрытия в 10;
  - Storybook, Flutter, версия всегда TODO — нужны поля в `ds/components.md`; a11y всегда `TODO: фронты`;
  - нет правил вёрстки: уровни заголовков, плашка vs инлайн TODO, ширина рядов; gotcha — `createAutoLayout` даёт белую заливку;
  - при переписывании — считать TODO до/после; итоговый блок «Мёртвые сочетания» в 4.
- **Предложение для скилла:** 0.3.0 после ревью Zhandos.
- **Применено в версии:** —
