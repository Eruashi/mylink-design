# FIXES-2026-07 — отчёт о выполненных правках

> Автоматическое исполнение утверждённого плана фиксов по `REVIEW-2026-07.md`.
> Скоуп: все 🔴-критичные проблемы + напрямую связанные с ними. Дата: 2026-07-02.
> Для финального ревью: ниже — каждый изменённый файл, что именно поменялось и зачем,
> принятые решения, результаты самопроверки. Запись в `CHANGELOG.md` — секция
> «2026-07-02 — Ревью-фиксы».

---

## Принятые решения (были заявлены в плане, применены)

1. **Создан `templates/skill-skeleton.md`** — вместо переписывания шага 3 CONTRIBUTING.
2. **Зеркала в `related` — с префиксом `mirrors/anthropic/<name>`** — нотация зафиксирована в SCHEMA §2, применена в 5 скиллах.
3. **«постер, плакат» удалены из таблицы триггеров** — запрос уходит в fallback `INDEX.md`; eval переписан под это ожидание. Взамен в строку graphic-design добавлен реальный триггер «визуальная иерархия».
4. **CHANGELOG — короткий формат** — одна датированная секция, 1–3 строки на изменение.

---

## Изменения по файлам

### PROJECT_INSTRUCTIONS.md (2.0.0 → 2.1.0) — канон роутинга

- **Шаг 2 переопределён:** таблица триггеров — «быстрый индекс, не арбитр»; при сомнении/коллизии решает `description` frontmatter и «Скилл не покрывает». Добавлены правила коллизий: «экран/фича сами по себе домен не выбирают» (логика → product-design, «почему тяжело» → ux-ui-theory, «как выглядит» → graphic-design); «спека компонента» → design-systems, handoff — только про передачу в разработку; «триггер сработал, но scope исключает → вне scope + fallback».
- **Иерархия:** слой 3 `/skills/meta/` удалён (5 уровней вместо 6); meta-скиллы явно объявлены зеркалами с путями `mirrors/anthropic/*`. Добавлено правило «Локальный скилл всегда побеждает зеркало» с требованием называть противоречия источников.
- **Прочие правила:** + «Ничего не триггерится» (цепочки → один вопрос → design-critique-fallback для «посмотри/оцени») — раньше это жило только в ROUTER и расходилось с PI; + «Отсутствующий или пустой файл — не стоп» (продолжай, пробел — строкой `не найдено: <путь>` в финальном блоке).
- **Таблица триггеров:** graphic-design без «постер, плакат», добавлена «визуальная иерархия».
- **Блок прозрачности:** `skills/<domain>/<skill-name> v<version>` → `skills/<domain> v<version>` (воспроизводимый формат под реальную одноуровневую структуру).
- **«Полная документация»:** описание ROUTER — «разборы конфликтных кейсов, логику не дублирует».

### ROUTER.md (2.0.0 → 3.0.0, breaking) — сокращён с 176 до 66 строк

- Оставлены **только** Случаи 1–5 с обоснованиями; шапка явно говорит: исполняемая логика — в PROJECT_INSTRUCTIONS.md, здесь не дублируется.
- Удалены дубли: таблица триггеров, алгоритм шагов, иерархия, цепочки, «поведение по умолчанию», блок прозрачности, «что роутер НЕ делает» (уникальные пункты влиты в Случаи 3 и 4).
- Пути meta починены: `/skills/meta/design-critique/` → `mirrors/anthropic/design-critique` (Случаи 1, 5).
- Случай 4 усилен реальным примером: `ux-copy` (3 варианта A/B/C) vs `ux-writing` (один вариант) — побеждает локальный скилл.
- `people/anna/` → `people/<name>/`.

### README.md

- Иерархия (строка «Принципы репозитория») — без `/skills/meta/`, зеркала помечены «(включая meta-зеркала)».
- Phase 1–3 помечены _(завершена)_; `skills/` в схеме структуры — без «(в работе)».

### CONTRIBUTING.md

- Шапка: удалены оба статусных блока, включая ложное «`evals/test-prompts.md` пока не создан».
- §1 шаг 2: одноуровневая структура `skills/<domain>/` как норма.
- §5: «Обновить `/personal/`» → «Обновить `/team/` или `/people/<имя>/`» с разными правилами для двух слоёв (people — личная зона; team — обсуждение + grep по скиллам-потребителям + Last updated).
- §6: пример коммита `personal: …` → `team/current-projects: …`.
- §7: ссылка `/personal/design-principles.md` → `team/design-principles.md`.

### SCHEMA.md

- Шапка: удалён статусный блок про паузу CHANGELOG.
- §1: норма — `skills/<domain>/SKILL.md` (один домен = один скилл); второй уровень — опция будущего дробления; ссылки на скиллы пишутся `skills/<domain>`.
- §2: `related` — нотация для зеркал (`mirrors/anthropic/<name>`); `mirrors` — всегда `null` в skills, признак зеркала — `ATTRIBUTION.md`; комментарий в YAML-примере обновлён.
- §6: снято невыполнимое требование «в frontmatter — `mirrors: <url>`» (противоречило «байт-в-байт»); признак зеркала — `ATTRIBUTION.md`; пункты перенумерованы.
- §7: пример записи CHANGELOG — реальный путь `skills/ux-writing`.

### templates/skill-skeleton.md — создан (1.0.0)

Болванка нового SKILL.md: frontmatter со всеми полями SCHEMA §2 и комментариями-подсказками (включая новую нотацию `related` и `mirrors: null`), все обязательные секции тела §3 с подсказками-цитатами, инструкция по использованию после frontmatter (чтобы YAML оставался первым блоком файла). Шаг 3 CONTRIBUTING §1 больше не ссылается в пустоту; добавлен в таблицу шаблонов INDEX.

### skills/ux-research/SKILL.md (1.0.0 → 1.0.1)

- `mirrors: [user-research, research-synthesis]` → `mirrors: null` (тип поля по SCHEMA); сигнал «использует зеркала» сохранён — оба перенесены в `related` с префиксом.
- Убраны оба «(когда будет)» про `skills/product-design` (Шаг 7 и Связи) — скилл существует с 2026-07-02.
- `last_updated: 2026-07-02`.

### skills/graphic-design, product-analytics, ux-ui-theory, ux-writing (все 1.0.0 → 1.0.1)

- `related`: `design-critique` → `mirrors/anthropic/design-critique` (имя больше не читается как несуществующий локальный скилл). ux-writing дополнительно получил `mirrors/anthropic/ux-copy` — зеркало, на котором построен его Шаг 2. Порядок: локальные скиллы, затем зеркала. `last_updated: 2026-07-02`.
- Не тронуты: `branding`, `design-systems`, `product-design` — в их `related` зеркал не было (проверено).

### evals/test-prompts.md

- «Как проверять» привязано к `PROJECT_INSTRUCTIONS.md` Шаг 2 (был ROUTER).
- Статус product-design: убрано ложное «формулировки пересекаются с триггерными словами» — честно зафиксировано, что кейс 1 ловится арбитражем по description (штатный путь).
- Кейс «спека компонента»: ожидание уточнено — «не meta/handoff», со ссылкой на правило коллизий.
- Кейс «постер»: ожидание — «вне доменов → fallback INDEX.md» (слова убраны из таблицы).
- Кейс «размер выборки A/B»: ожидание приведено к новой логике — «триггерится → честный отказ по scope» (та же двусмысленность, что была у постера).
- Кейс «нейминг»: комментарий дополнен правилом «фича сама по себе домен не выбирает».
- **Новая секция `meta`** (3 кейса): ревью перед lead review → design-critique; доступность формы → accessibility-review; спецификация для фронтендеров → design-handoff (+ граница с design-systems по правилу «спека»).
- **Новая секция «Спец-кейсы»** (3 кейса): мульти-домен (пример из ROUTER Случая 1), «ничего не триггерится» → один уточняющий вопрос, цепочка «кнопка не конвертит».
- Итог покрытия обновлён: 8 доменов + meta + спец-кейсы; честно сохранено «реального прогона через сессию не было».

### INDEX.md

- Шапка: «все raw-ссылки готовы к web_fetch» → «локальные файлы читаются напрямую, web_fetch — только внешние ссылки (Шаг 6)» — снято противоречие с PROJECT_INSTRUCTIONS.
- Навигация: `personal/` → `team/, people/, …, evals/`.
- Удалена строка «База: raw.githubusercontent…» над локальными ресурсами (приглашала фетчить локальное по сети).
- Секция evals актуализирована (meta и спец-кейсы покрыты).
- В таблицу шаблонов добавлен `skill-skeleton.md`; футер: актуальный состав ресурсов, дата 2026-07-02.

### CHANGELOG.md

- Добавлена секция «2026-07-02 — Ревью-фиксы: роутинг, meta-слой, синхронизация документов» — все изменения этого прохода, короткими строками, с Notes про перепрогон evals.

---

## Самопроверка (все прогоны — после правок)

### Grep-проверки

| Проверка | Результат |
|---|---|
| `/skills/meta` в живых документах (PI, ROUTER, README, SCHEMA, CONTRIBUTING, INDEX, skills/, evals/) | ✅ пусто (единственное упоминание в PI — явное «такого слоя не существует») |
| `personal/` в живых документах (вне people/) | ✅ пусто (историческая секция CHANGELOG и README-история Phase 0 не тронуты сознательно) |
| «(когда будет)» в skills/ | ✅ пусто |
| `templates/skill-skeleton.md` существует, ссылки CONTRIBUTING/INDEX живые | ✅ |
| «постер» в таблице триггеров PI | ✅ пусто |
| `anna` в PI/ROUTER | ✅ пусто |
| ROUTER: дубли таблиц (триггеры/цепочки/прозрачность) | ✅ 0 вхождений; 176 → 66 строк |
| Frontmatter всех 8 скиллов: `mirrors: null`, версии, нотация `related` | ✅ 5 скиллов 1.0.1 с префиксами; branding / design-systems / product-design не тронуты (зеркал в related не имели) |

### Ручной перепрогон evals по новому Шагу 2 (все 35 кейсов)

Проблемные кейсы из ревью — до/после:

| Кейс | Было (по старой таблице) | Стало (по новому Шагу 2) |
|---|---|---|
| Размытое ТЗ про график (product-design №1) | промах — ни одного слова из таблицы | ✅ арбитраж по description («requirements are vague») — штатный путь, зафиксирован в статусе секции |
| «Собери спеку компонента Date picker» | коллизия: design-systems + meta/handoff | ✅ design-systems; handoff отсечён правилом «спека» |
| «Почему этот экран ощущается тяжёлым» | ложный домен: «экран» → product-design | ✅ ux-ui-theory — правило «почему тяжело» |
| «Поправь визуальную иерархию на экране» | тройная коллизия (экран/иерархия) | ✅ graphic-design — прямой триггер «визуальная иерархия» + правило «как выглядит» |
| «Сделай постер» | противоречие: таблица роутит, eval запрещает | ✅ вне доменов → fallback INDEX; ожидание eval переписано |
| «Придумай название для фичи» | ложный триггер «фичи» → product-design | ✅ «фича» домен не выбирает; branding-scope исключает нейминг → отказ |
| «Размер выборки для A/B» | скрытая коллизия («A/B» триггерит) | ✅ триггерится → честный отказ по scope; ожидание приведено к фактическому поведению |
| Остальные 28 кейсов (включая 6 новых: meta ×3, мульти-домен, пустой триггер, цепочка) | — | ✅ разруливаются однозначно: прямые триггеры таблицы, цепочки или точные совпадения с description |

Пограничное наблюдение (не блокер): «Экран ощущается не так, но не могу понять, что именно» (graphic-design №1) пересекается с description ux-ui-theory («why does this screen feel hard»), но выигрывает graphic-design за счёт дословного совпадения с его description («this screen feels off but I don't know why»). Если на практике начнёт уводить в теорию — усилить формулировку в description одного из двух скиллов.

**Не проверено (как и раньше, честно):** реальный прогон через рабочую сессию с роутером — ручная сверка не заменяет его. Это единственный оставшийся шаг валидации, и он на Zhandos.

---

## Что сознательно НЕ тронуто (вне скоупа критики — ждёт отдельного решения)

- Хардкод Zhandos / Екатерины в 6 скиллах — стратегический вопрос (поднимать принципы в team или параметризовать через people).
- `[~]`-нотация и пропавший `empty` в `team/design-principles.md`.
- Чистка `people/Zhandos/tone-of-voice.md` от каркаса опросника (~3–4 КБ каждой сессии).
- COWORK-секция / MODES.md для двойного режима.
- Пустые `EXTENSIONS.md` у зеркал; мусорный `people/Assema/test`; опечатки `people/README.md`.
- Разрезание INDEX на локальный/внешний и `current-projects.md` по продуктам.

Полный список — `REVIEW-2026-07.md`, разделы Quick wins (пп. 6–7, 9) и «Стратегические улучшения».
