# Modal header — статус и решения

Файл: Core kit, ветка `bRwFkumYDoKh1OQv5a5dYp`, секция `23604:2676` (страница ✅ Tabs), гайдлайн `23608:1159`. Компонент в main — `17100:18553`.
Обновлено: 2026-09-26.

## Сделано в сессии 2026-09-26
- Гайдлайн по `ds/guidelines/modal-header.md` (ds-guideline 0.1.0) свёрстан в ветке: корневой фрейм `23608:1159` (1440 × 9898), 13 секций `01 — Обзор` … `13 — Делаем и не делаем` + «Мета» и «Итог».
- Примеры — инстансы `Modal header` и `Close button`; 26 пометок `TODO: проверить` подсвечены плашками `bg/warning/secondary-light`.
- Все цвета и текстовые стили — из 🧩 Tokens. Без токенов: padding корня 80, зазор секций 72, вертикальный padding ячеек 10.
- Section 1: 1740 × 2894 → 1740 × 10098. В ветку импортированы ссылки на 17 текстовых стилей и ~35 переменных Tokens. Вне секции ничего не менялось.

- Перестилизовано под пример «Tabs/Segment controls» `19500:12562`: фрейм `23608:1159` 1440 × 9898 → 936 × 9657, обложка — копия `19500:12563` с превью Modal header, таблицы в карточках, TODO — жёлтые плашки. Структура 13 секций и контент не менялись. Section 1 → 1236 × 9857. Текстовые стили Tokens сняты (размеры и цвета заданы как в примере Tabs).

- Пересобрано по новой структуре из 10 разделов (ds-guideline 0.2.0): фрейм `23608:1159` 936 × 9657 → 936 × 15161; разделы `23629:1348` … `23629:1384`; в «Свойствах» 13 рядов примеров; TODO 50 (26 прежних + 24 для фронтов/мобильщиков). Section 1 → 1236 × 15361.

- v2, минималистичная: фрейм `23637:1666` (1600 × 9653) справа от старого `23608:1159` (старый не менялся — для сравнения). Две колонки на раздел, примеры на подложках, без оранжевого текста; 8 открытых вопросов в конце раздела 10, остальные — в `ds/guidelines/modal-header.md`. Section 1 → 3036 × 15361.

## Осталось вручную
- Визуальное ревью Zhandos; ответы на вопросы в таблице ниже.

## Отдельные таски
1. Мёртвые свойства: `⬅️ Back`, `✅ Status` при `Align=Center`; `🏞️ Illustration` при `Align=Left`; `🔘 Sheet header` на Web — разделить компонент или оставить.
2. Длинный Title заходит под `Close button` — правый паддинг у `Top`.
3. Статус на `OLD Badge` → `Badge` `23457:2960`.
4. Паддинги: Web md Center 24 vs md Left 20; Mobile Center без боковых; Mobile lg Description 12 (Left) vs 14 (Center).
5. В `ds/components.md` гайдлайн «нет», но на странице есть свободный гайд `19442:22424` — учесть при перевыгрузке реестра.

## Отличия от эталона Sidebar
- Типографика по стилям Tokens: H1 `Heading/H2` (Bold, в эталоне Semi Bold), абзацы `Body/md-regular` 16/22 (в эталоне 15/24), ячейки `Body/sm-regular` 14/20 (в эталоне 13/20).
- Не отрисованы: превью бейджа в таблице 08, пример «Применить изменения?» из 01.

## Открытые вопросы
- Title: до 2 строк (компонент) или одна строка (свободный гайд)? → Zhandos
