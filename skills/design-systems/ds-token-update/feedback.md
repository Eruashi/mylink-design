# Фидбек: ds-token-update

Заполняется после каждого реального прогона. Раз в 3–5 записей Claude Code предлагает дифф в `SKILL.md` со ссылками на записи.

## 2026-09-26 — Counter-badge `8454:7170`, аудит (тестовый прогон)
- **Что сработало:** скрипт прошёл 657/657 полей, 42/42 варианта; итог совпал с `ds/components.md`; 612 полей с высокой уверенностью, 30 — «нет в источнике»; попутно найден риск контраста.
- **Что поправил руками:** —
- **Чего не хватило:**
  - колонки в SKILL.md и `templates/review-report.md` разные;
  - скрипт не отличает локальную копию семантики от локального примитива (оба `old`);
  - нет правил уверенности для solid ↔ alpha и state-токенов (`-hover`, `-disabled`), цветовая роль (neutral vs pale) не учитывается;
  - нет правила группировки строк и правила «TEXT без стиля → текстовый стиль Tokens целиком»;
  - нет карты переименований числовых токенов (`Border/border-xs` → `border-xs`);
  - скрипт не ловит «значение переменной ≠ значение ноды» и непривязанные width/height;
  - не сказано, где искать связанные таски (упоминание в чужом status-файле), какой LIMIT безопасен, что делать с контрастом, кто пишет feedback в режиме аудита.
- **Предложение для скилла:** закрыть пункты выше в 0.2.0 (SKILL.md + скрипт).
- **Применено в версии:** 0.2.0 (SKILL.md + скрипт аудита)

## 2026-09-26 — Counter-badge `8454:7170`, аудит повторно (0.2.0)
- **Что сработало:** новые классы и флаг `mismatch` дали то, что в 0.1.0 искалось руками (примитивы Pale blue/Gray/Purple, двойники Border, высота lg 16 ≠ 20, min/max без переменных); 783/783 поля; 19 строк вместо 21.
- **Что поправил руками:** скрипт считал находкой HUG-размер корня (~250 ложных полей) → находка только при FIXED.
- **Чего не хватило:**
  - группировка: объединять поля с одним решением (padding L/R/T/B, min/max), не только цвета; агрегат `padding` в скрипте;
  - что писать в «Предлагаю» и «Решение» для `mismatch`; как быть с привязкой размера у HUG-ноды в отключённую библиотеку;
  - схема имени для новых числовых/размерных токенов;
  - `-disabled` как вариант (`bg/neutral/disabled`) — state-токен или нет; «≤ 1 шаг палитры» при промежуточных шагах;
  - M в строке покрытия — без n/a; подложка и порог для контраста alpha-фона 8–10 px;
  - повторный прогон: сверять с прошлым status-файлом и показывать разницу; место для «Связанных тасок» в шаблоне;
  - скрипт ~30 КБ вставляется в каждый вызов — нужна компактная версия.
- **Предложение для скилла:** закрыть в 0.3.0 после 1–2 прогонов на других компонентах.
- **Применено в версии:** —
